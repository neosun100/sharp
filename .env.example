# SHARP Service Configuration

# Service port (check available ports before setting)
PORT=8080

# Host IP binding (0.0.0.0 for all interfaces)
HOST_IP=0.0.0.0

# GPU idle timeout in seconds (auto-release VRAM after idle)
GPU_IDLE_TIMEOUT=300

# GPU device ID (auto-selected by start.sh)
NVIDIA_VISIBLE_DEVICES=0

# Data directory for input/output files
DATA_DIR=/tmp/sharp
